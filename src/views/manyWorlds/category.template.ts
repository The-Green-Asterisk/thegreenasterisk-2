import { html } from "@elements";

const categoryTemplate = () => html`
    <div id="world-content">
        <div id="category-entities">
            <p>No entities available for this category.</p>
        </div>
    </div>
`;

export default categoryTemplate;